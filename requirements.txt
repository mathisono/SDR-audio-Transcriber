# MSE-88 may be on an older distro Python. Python 3.8 needs older compatible wheels.
# faster-whisper 0.10.1 requires av==10.* and ctranslate2 >=3.22,<4.
# PyAV 10 source builds need an older Cython on older distro environments.
Cython==0.29.37
av==10.*
ctranslate2==3.24.0
tokenizers>=0.13,<0.16
onnxruntime>=1.14,<1.18
faster-whisper==0.10.1
requests>=2.31.0
